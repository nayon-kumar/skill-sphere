import ProfileClient from "@/components/Profile/ProfileClient";
export const metadata = {
  title: "User Profile",
  description: "Our User Profile",
};
const ProfilePage = () => {
  return <ProfileClient />;
};

export default ProfilePage;
